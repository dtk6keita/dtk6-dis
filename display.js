const Display={currentView:null,slideIndex:0,lastSwitch:0,showClockNext:false,lastMode:null,init(){this.views={clock:document.getElementById("clockView"),slide:document.getElementById("slideView"),message:document.getElementById("messageView")};this.slideImage=document.getElementById("slideImage");this.slidePlaceholder=document.getElementById("slidePlaceholder");this.slideTitle=document.getElementById("slideTitle");this.slideMessage=document.getElementById("slideMessage");this.showView("clock")},showView(name){if(this.currentView===name)return;Object.values(this.views).forEach(v=>v.classList.remove("active"));this.views[name].classList.add("active");this.currentView=name},setModeClass(mode){document.body.className=`mode-${mode}`},statusText(mode){return{normal:"NORMAL",checkin:"CHECK-IN",loading:"ALERT",depart:"DEPART NOW",safe:"SAFE DRIVE"}[mode]||"NORMAL"},updateInfo(state){document.getElementById("offer").textContent=`${state.wave} OFEER`;document.getElementById("status").textContent=this.statusText(state.mode);document.getElementById("otdTime").textContent=state.otdLimit;document.getElementById("remaining").textContent=state.remaining},showMessage(mode){const msg=CONFIG.messages[mode];document.getElementById("messageStatus").textContent=msg.title;document.getElementById("messageText").textContent=msg.text;this.showView("message")},showSlide(){const slide=CONFIG.slides[this.slideIndex];this.slideTitle.textContent=slide.title;this.slideMessage.textContent=slide.message;this.slidePlaceholder.style.display="flex";this.slideImage.style.display="none";this.slideImage.onload=()=>{this.slideImage.style.display="block";this.slidePlaceholder.style.display="none"};this.slideImage.onerror=()=>{this.slideImage.style.display="none";this.slidePlaceholder.style.display="flex"};this.slideImage.src=slide.src;this.slideIndex=(this.slideIndex+1)%CONFIG.slides.length;this.showView("slide")},updateMainView(state){if(state.mode!==this.lastMode){this.lastMode=state.mode;this.lastSwitch=0;this.showClockNext=false}if(state.mode!=="normal"){this.showMessage(state.mode);return}const now=Date.now();if(!this.lastSwitch||now-this.lastSwitch>=CONFIG.slideIntervalSeconds*1000){this.lastSwitch=now;if(this.showClockNext){this.showView("clock")}else{this.showSlide()}this.showClockNext=!this.showClockNext}}};
